// Here we can inport schema from GitHub API, 
// so we will have typing available for available 
// queries and entity interfaces
import { schema } from '@octokit/graphql-schema'
export default schema.json;
